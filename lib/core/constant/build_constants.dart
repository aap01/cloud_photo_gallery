import 'package:flutter/foundation.dart';

class BuildConstants {
  BuildConstants._();
  static const debug = kDebugMode;
  static const release = kReleaseMode;
  static const profile = kProfileMode;
}
